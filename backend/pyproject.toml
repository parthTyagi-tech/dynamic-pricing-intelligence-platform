[project]
name = "dynamic-pricing-intelligence-api"
version = "0.1.0"
description = "Klypup Pricing Intelligence Flask API"
requires-python = ">=3.11"
dependencies = []

[tool.vercel]
entrypoint = "run:app"
