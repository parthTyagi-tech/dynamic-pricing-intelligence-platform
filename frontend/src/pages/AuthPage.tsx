import { AnimatePresence, motion } from "framer-motion";
import { ArrowRight, Check, Eye, EyeOff, GitBranch, Globe2, LockKeyhole, Mail, Sparkles, UserRound, X, Zap } from "lucide-react";
import { FormEvent, useState } from "react";
import { useLocation, useNavigate } from "react-router-dom";
import { useAuth } from "../context/AuthContext";
import { Badge, Button, GlassCard, useToasts, ToastStack } from "../components/ui";

export default function AuthPage() {
  const navigate = useNavigate();
  const location = useLocation();
  const { login, signup } = useAuth();
  const { toasts, push, dismiss } = useToasts();
  const [mode, setMode] = useState<"signin" | "signup">(location.pathname === "/signup" ? "signup" : "signin");
  const [showPassword, setShowPassword] = useState(false);
  const [forgotOpen, setForgotOpen] = useState(false);
  const [remember, setRemember] = useState(true);
  const [busy, setBusy] = useState(false);
  const [form, setForm] = useState({ name: "", email: "", password: "", organization: "Klypup Enterprise" });
  const [errors, setErrors] = useState<Record<string, string>>({});
  const validate = () => { const next: Record<string, string> = {}; if (mode === "signup" && form.name.trim().length < 2) next.name = "Enter your full name."; if (!/^\S+@\S+\.\S+$/.test(form.email)) next.email = "Use a valid work email."; if (form.password.length < 6) next.password = "Use at least 6 characters."; setErrors(next); return !Object.keys(next).length; };
  const submit = async (event: FormEvent) => { event.preventDefault(); if (!validate()) return; setBusy(true); try { if (mode === "signin") await login(form.email, form.password); else await signup(form); navigate("/dashboard"); } finally { setBusy(false); } };
  const switchMode = (next: "signin" | "signup") => { setMode(next); setErrors({}); navigate(next === "signin" ? "/login" : "/signup", { replace: true }); };
  return <div className="auth-page"><ToastStack toasts={toasts} dismiss={dismiss} /><div className="auth-visual"><div className="auth-orb auth-orb-one" /><div className="auth-orb auth-orb-two" /><div className="auth-grid" /><div className="auth-brand"><span className="brand-glyph"><Sparkles size={18} /></span><span><strong>KLYPUP<span>AI</span></strong><small>PRICING INTELLIGENCE</small></span></div><div className="auth-visual-copy"><Badge tone="indigo" dot>Decision intelligence, refined</Badge><h1>Move with the market.<br /><em>Lead with the signal.</em></h1><p>Premium pricing intelligence for teams that want to turn every price point into a strategic advantage.</p></div><div className="auth-signal-grid"><div><Zap size={16} /><span>1,284</span><small>SKUs repriced</small></div><div><Sparkles size={16} /><span>+7.8%</span><small>margin lift</small></div><div><Globe2 size={16} /><span>94.2%</span><small>forecast confidence</small></div></div><div className="auth-quote">“Klypup gives our operators the clarity to move quickly, without losing the story behind the decision.”<small>— Maya Chen, VP Growth at Northline</small></div></div><div className="auth-form-side"><div className="auth-form-wrap"><div className="mobile-auth-brand"><span className="brand-glyph"><Sparkles size={18} /></span><strong>KLYPUP<span>AI</span></strong></div><div className="auth-heading"><p className="eyebrow">Welcome to the workspace</p><h2>{mode === "signin" ? "Good to see you again." : "Build your intelligence layer."}</h2><p>{mode === "signin" ? "Sign in to continue managing your pricing system." : "Create your workspace and start making sharper pricing decisions."}</p></div><div className="auth-tabs"><button type="button" className={mode === "signin" ? "active" : ""} onClick={() => switchMode("signin")}>Sign in</button><button type="button" className={mode === "signup" ? "active" : ""} onClick={() => switchMode("signup")}>Create account</button></div><form onSubmit={submit} className="auth-form">{mode === "signup" && <label className="field"><span>Full name</span><div className="field-input"><UserRound size={16} /><input value={form.name} onChange={(event) => setForm({ ...form, name: event.target.value })} placeholder="Kiran Evans" /> </div>{errors.name && <small className="field-error">{errors.name}</small>}</label>}<label className="field"><span>Work email</span><div className="field-input"><Mail size={16} /><input type="email" value={form.email} onChange={(event) => setForm({ ...form, email: event.target.value })} placeholder="you@company.com" /> </div>{errors.email && <small className="field-error">{errors.email}</small>}</label>{mode === "signup" && <label className="field"><span>Organization</span><div className="field-input"><Globe2 size={16} /><input value={form.organization} onChange={(event) => setForm({ ...form, organization: event.target.value })} placeholder="Company name" /> </div></label>}<label className="field"><span>Password</span><div className="field-input"><LockKeyhole size={16} /><input type={showPassword ? "text" : "password"} value={form.password} onChange={(event) => setForm({ ...form, password: event.target.value })} placeholder="••••••••" /><button type="button" className="field-icon-action" onClick={() => setShowPassword(!showPassword)} aria-label={showPassword ? "Hide password" : "Show password"}>{showPassword ? <EyeOff size={16} /> : <Eye size={16} />}</button></div>{errors.password && <small className="field-error">{errors.password}</small>}</label>{mode === "signin" && <div className="form-options"><label className="remember"><input type="checkbox" checked={remember} onChange={(event) => setRemember(event.target.checked)} /><span>Remember me</span></label><button type="button" className="text-button" onClick={() => setForgotOpen(true)}>Forgot password?</button></div>}<Button type="submit" disabled={busy} className="auth-submit">{busy ? "Connecting…" : mode === "signin" ? <>Enter command center <ArrowRight size={16} /></> : <>Create workspace <ArrowRight size={16} /></>}</Button></form><div className="divider"><span>or continue with</span></div><div className="oauth-grid"><Button variant="secondary" onClick={() => push("Google connection is ready for your workspace.", "info")}><span className="oauth-google">G</span> Google</Button><Button variant="secondary" onClick={() => push("GitHub connection is ready for your workspace.", "info")}><GitBranch size={16} /> GitHub</Button></div><p className="auth-legal">By continuing, you agree to Klypup’s <button type="button" className="text-button">Terms</button> and <button type="button" className="text-button">Privacy Policy</button>.</p></div></div><AnimatePresence>{forgotOpen && <div className="drawer-backdrop" onClick={() => setForgotOpen(false)}><motion.aside initial={{ x: "100%" }} animate={{ x: 0 }} exit={{ x: "100%" }} className="reset-drawer" onClick={(event) => event.stopPropagation()}><button className="icon-button drawer-close" onClick={() => setForgotOpen(false)} aria-label="Close reset drawer"><X size={18} /></button><p className="eyebrow">Account recovery</p><h2>Reset your password.</h2><p>Enter your work email and we’ll send a secure reset link.</p><div className="field"><span>Work email</span><div className="field-input"><Mail size={16} /><input placeholder="you@company.com" /></div></div><Button onClick={() => { setForgotOpen(false); push("If that email exists, a secure reset link is on its way.", "info"); }}><Check size={16} /> Send reset link</Button></motion.aside></div>}</AnimatePresence></div>;
}
